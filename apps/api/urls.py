"""
URL routing for API endpoints.

When you implement API views (e.g. using Django REST Framework), include
their URL patterns here and import them into the project's root urls.py.
"""

from django.urls import path, include

urlpatterns: list = [
    # Example:
    # path("events/", include("apps.events.api_urls")),
]
